public class Constants {

    public enum KeyBoard {
        CLEAR_ALL, CLEAR, BACKSPACE, DIVIDE,
        SEVEN, EIGHT, NINE, MULTIPLY,
        FOUR, FIVE, SIX, MINUS,
        ONE, TWO, THREE, PLUS,
        SIGN, ZERO, DOT, EQUAL
    }
}