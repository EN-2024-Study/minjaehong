namespace Library
{
    // MODE AND MENU STATE
    public enum LibraryMode { USER_MODE = 0, MANAGER_MODE = 1, PROGRAM_EXIT = 2 };

    public enum UserFrontMode { GO_BACK = -1, USER_LOGIN = 0, USER_CREATE_ACCOUNT = 1 };

    public enum ManagerMenuState
    {
        GOBACK = -1, PRINTALLBOOK, FINDBOOK, ADDBOOK, DELETEBOOK, UPDATEBOOK,
        PRINTALLMEMBER, BORROWHISTORY, NAVERSEARCH, LOGMANAGEMENT, APPLYREQUESTED
    };

    public enum UserMenuState
    {
        GOBACK = -1, PRINTALLBOOK, FINDBOOK, BORROW, CHECKBORROW, RETURN, CHECKRETURN, UPDATEMYINFO,
        DELETEMYSELF, NAVERSEARCH, PRINTREQUESTED
    };

    public enum LoggerMenuState
    {
        GOBACK = -1, DELETELOG, SAVELOG, DELETELOGFILE, RESETLOG
    }

    public enum ExceptionState
    {
        // FORMAT EXCEPTION
        INT_ONLY, ENGLISH_ONLY, ENGLISH_INT_ONLY, KOREAN_ENGLISH_INT_ONLY, PHONENUM_ONLY, ISBN_ONLY, DATE_ONLY,

        // RUNTIME EXCEPTION
        EXISTING_ID, NONEXISTING_ID, DONT_HAVE_CERTAIN_BOOK, NOT_ENOUGH_QUANTITY

    };
}